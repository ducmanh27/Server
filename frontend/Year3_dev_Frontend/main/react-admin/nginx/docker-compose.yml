version: '3.8'
services:
  nginx:
    build: 
      context: .
    ports:
      - 3000:80
    volumes:
      - ../build:/usr/share/nginx/html
      # - ./nginx.conf:/etc/nginx/conf.d/nginx.conf
      #docker compose -p smartfarm-nginx up